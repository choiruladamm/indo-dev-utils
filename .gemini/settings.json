{
  "hooks": {
    "AfterTool": [
      {
        "description": "Run automated check after file modification",
        "command": "pnpm -w lint:fix && pnpm --filter @indodev/toolkit typecheck && pnpm --filter @indodev/toolkit test:run",
        "if": "tool.name == 'replace' || tool.name == 'write_file'",
        "allow_fail": true
      }
    ]
  }
}
